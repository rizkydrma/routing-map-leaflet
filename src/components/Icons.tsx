import { XCircleIcon, PlusCircleIcon, MapPin, ChevronRight, Loader2 } from 'lucide-react';

export const Icons = {
  XCircleIcon,
  PlusCircleIcon,
  MapPin,
  ChevronRight,
  Loader2,
};

export default Icons;
